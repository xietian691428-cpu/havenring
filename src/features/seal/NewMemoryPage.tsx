"use client";

import type { NewMemorySealComposerProps } from "./sealTypes";

/**
 * Future home for the Capture / **New Memory** composer.
 * Until migration, implementations live under `views/NewMemoryPage.js`.
 */

export type NewMemoryPageSealPropShape = NewMemorySealComposerProps;
